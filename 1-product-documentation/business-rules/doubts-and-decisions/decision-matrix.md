# Dashboard : Product Documentation - Business Rules

Decision Id index only (current ids). Supersede and merge chains live in [history/](history/).

Add one `## BR-XX.YY` section per indexed business rule **owned by this block**. Under each section:

| Event (brief) | Decision Id |
|---------------|-------------|
