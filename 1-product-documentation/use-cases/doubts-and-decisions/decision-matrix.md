# Dashboard : Product Documentation - Use Cases

Decision Id index only (current ids). Supersede and merge chains live in [history/](history/).

Add one `## UC-XX` section per indexed use case **owned by this block**. Under each section:

| Event (brief) | Decision Id |
|---------------|-------------|
