# Decision matrix — Layer 1 product documentation

Vigente decisions only. Supersede and merge chains live in [history/](history/).

Add one `## {element-id}` section per indexed element **owned by this block**. Under each section:

| Event (brief) | Vigente doubt |
|---------------|---------------|
