# Decision matrix — logical-domain

Effective decisions only. Supersede and merge chains live in [history/](history/).

Add one `## {entity-name}` section per indexed entity **owned by this block**. Under each section:

| Event (brief) | Effective doubt |
|---------------|---------------|
