# Dashboard : Product Documentation - Logical Domain

Decision Id index only (current ids). Supersede and merge chains live in [history/](history/).

Add one `## {entity-name}` section per indexed entity **owned by this block**. Under each section:

| Event (brief) | Decision Id |
|---------------|-------------|
