# Dashboard : Product Documentation - Diagrams C4 - L4 Critical Zones

Decision Id index only (current ids). Supersede and merge chains live in [history/](history/).

Add one `## ZC-XX` section per indexed critical zone **owned by this block**. Under each section:

| Event (brief) | Decision Id |
|---------------|-------------|
