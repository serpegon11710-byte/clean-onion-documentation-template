# Decision matrix — L4-critical-zones

Effective decisions only. Supersede and merge chains live in [history/](history/).

Add one `## ZC-XX` section per indexed critical zone **owned by this block**. Under each section:

| Event (brief) | Effective doubt |
|---------------|---------------|
