# Dashboard : Product Documentation - Diagrams C4

Decision Id index only (current ids). Supersede and merge chains live in [history/](history/).

Add one `## {element-id}` section per indexed diagram element **owned by this block**. Under each section:

| Event (brief) | Decision Id |
|---------------|-------------|
